import 'package:flutter/material.dart';


class AppThemeChangeScreen extends StatelessWidget {
  const AppThemeChangeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFF001128),
      // appBar: AppBar(),
    );
  }
}